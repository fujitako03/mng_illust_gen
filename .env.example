# OpenAI API Key（必須）
# https://platform.openai.com/api-keys で取得
OPENAI_API_KEY=your_openai_api_key_here

# Flask settings
# 開発環境: development, 本番環境: production
FLASK_ENV=development

# デバッグモード（開発時のみ有効にする）
# 1=有効, 0=無効
FLASK_DEBUG=1

# セキュリティ用のシークレットキー（任意の文字列）
# 本番環境では必ず変更してください
SECRET_KEY=your_secret_key_for_development

# ポート番号（デフォルトは5000）
# PORT=5000